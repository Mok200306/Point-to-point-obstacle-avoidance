# Gate 5 启动编排修复与 Smoke 复测报告

日期：2026-08-30  
任务书：`/home/w417/文档/Oracle预测式导航生死实验_分阶段执行任务书_v1.docx`  
当前分支：`exp/oracle-g5-stability-2026-08-30`  
修复提交：`a20b43cefce004ba6d0b25572c9cb9ed2035a72d`  
正式 Gate 5 状态：**BLOCKED**

## 1. 本次只修改了什么

本次只修改了实验启动编排脚本：

```text
experiments/oracle_mppi/scripts/run_gate2_scene.sh
```

修改前，`wait_for_nav2()` 只把以下三个节点全部 active 作为可发 Goal 条件：

```text
/controller_server
/planner_server
/collision_monitor
```

修改后，增加了 `/bt_navigator`，只有以下四个节点全部为 `active [3]` 才允许开始导航：

```text
/controller_server
/planner_server
/bt_navigator
/collision_monitor
```

这属于启动可靠性修复，不改变 MPPI 速度、采样时域、footprint、代价地图、
Oracle 网格、PredictionCritic 权重、场景文件或 Reactive/Oracle 的信息变量。

静态检查已通过：

```bash
bash -n experiments/oracle_mppi/scripts/run_gate2_scene.sh \
  experiments/oracle_mppi/scripts/run_gate5_smoke_matrix.sh
git diff --check
```

## 2. 复测目录与命令

### 2.1 `_02`：修复前提交上的独立 smoke

目录：

```text
experiments/oracle_mppi/gate5/stability_smoke_20260830_02/
```

该目录是在修复内容尚未提交时运行，证据中的实验 commit 为父提交
`d42fd67492296d7b77f0eeada47286839cd76908`。命令为：

```bash
./experiments/oracle_mppi/scripts/run_gate5_smoke_matrix.sh \
  --runs 1 \
  --root experiments/oracle_mppi/gate5/stability_smoke_20260830_02 \
  --startup-timeout 120 \
  --goal-timeout-seconds 300
```

### 2.2 `_03`：修复提交上的独立 smoke

目录：

```text
experiments/oracle_mppi/gate5/stability_smoke_20260830_03/
```

命令为：

```bash
./experiments/oracle_mppi/scripts/run_gate5_smoke_matrix.sh \
  --runs 1 \
  --root experiments/oracle_mppi/gate5/stability_smoke_20260830_03 \
  --startup-timeout 120 \
  --goal-timeout-seconds 300
```

两组目录均不可覆盖，失败 run 也保留。每个完成的 run 包含
`metrics.yaml`、`experiment.yaml`、`trajectory.csv`、
`trajectory_comparison.png`、`dynamic_trajectory_comparison.png`、
`gate5_timeline.png`、`cmd_vel.csv`、运行日志和 Gazebo contacts 压缩日志。

## 3. Smoke 结果

### 3.1 `_02` 结果

机器可读汇总：

- [gate5_smoke_summary.csv](../gate5/stability_smoke_20260830_02/gate5_smoke_summary.csv)
- [gate5_paired_summary.csv](../gate5/stability_smoke_20260830_02/gate5_paired_summary.csv)
- [matrix_status.csv](../gate5/stability_smoke_20260830_02/matrix_status.csv)

| 场景 | 方法 | 结果 | 仿真时间 [s] | 轨迹长度 [m] | 末端误差 [m] | 动态 contacts |
|---|---|---|---:|---:|---:|---|
| S1 横穿 | Reactive | PASS | 162.2 | 17.823 | 0.0315 | 无 |
| S1 横穿 | Oracle | PASS | 161.1 | 18.441 | 0.0218 | 无 |
| S2 对向 | Reactive | PASS | 177.4 | 18.471 | 0.0833 | 无 |
| S2 对向 | Oracle | PASS | 162.4 | 18.412 | 0.0441 | 无 |

结果：4/4 runner-level 通过，0 次启动失败，0 次动态碰撞，Oracle 消息校验均为 0。

### 3.2 `_03` 结果：修复提交复现

机器可读汇总：

- [gate5_smoke_summary.csv](../gate5/stability_smoke_20260830_03/gate5_smoke_summary.csv)
- [gate5_paired_summary.csv](../gate5/stability_smoke_20260830_03/gate5_paired_summary.csv)
- [matrix_status.csv](../gate5/stability_smoke_20260830_03/matrix_status.csv)

| 场景 | 方法 | 结果 | 仿真时间 [s] | 轨迹长度 [m] | 末端误差 [m] | 动态 contacts |
|---|---|---|---:|---:|---:|---|
| S1 横穿 | Reactive | PASS | 160.1 | 18.350 | 0.0441 | 无 |
| S1 横穿 | Oracle | **NAV_TIMEOUT** | 223.3 | 18.358 | 0.3012 | 无 |
| S2 对向 | Reactive | PASS | 173.5 | 18.517 | 0.0490 | 无 |
| S2 对向 | Oracle | PASS | 121.2 | 18.257 | 0.0381 | 无 |

结果：3/4 runner-level 通过；唯一失败为 S1 Oracle 导航超时，不是 Nav2 启动失败，
也不是 Gazebo 动态碰撞。

失败证据：

- `S1_crossing/oracle_run_01/metrics.yaml`：`nav2_status=5`、
  `goal_timed_out=true`、`final_xy_error_m=0.3012089660`；
- `S1_crossing/oracle_run_01/launch.log`：出现多次
  `GridBased: failed to create plan, no valid path found` 和
  `Failed to make progress`；
- `experiment.yaml`：`startup_failure` 未置为 true，
  `oracle_message_validation_exit=0`；
- `runtime_topics_and_lifecycle.txt`：四个要求节点均为 `active [3]`；
- Gazebo contacts 判定：`gazebo_robot_dynamic_contact=false`、
  `gazebo_non_ground_contact=false`。

因此，该失败应归类为 **Oracle 闭环控制/重规划收敛问题**，不能再归类为启动竞态。

## 4. 启动可靠性结论

在 `_02` 和提交版本 `_03` 的 8 个 smoke run 中：

- 所有进入导航阶段的 run 都记录了四个节点的 `active [3]` 快照；
- 没有再次出现 formal batch 中的 `planner_server` / `smoother_server`
  lifecycle response timeout 导致的启动失败；
- `_02` 为 4/4，`_03` 为 3/4；
- 合并计数为 7/8，但这两组是诊断 smoke，不是正式重复统计，不能据此宣称
  Gate 5 通过或 Oracle 有统计收益。

启动修复解决的是“何时允许发送 Goal”的判定缺口；它没有改变控制器在 S1
横穿场景中接近目标后的规划/停滞行为。后者需要单独诊断。

## 5. Oracle 链路结论

`_03` 两个 Oracle run 均满足：

- publisher 输出 `Gate 3 Oracle ready`；
- `oracle_message_validation.txt` 返回 0；
- `PredictionCritic status=active` 持续出现；
- 没有 stale 失败或接口校验失败；
- S2 Oracle 成功到达且无动态 contacts。

这证明当前提交上的 Oracle publisher 与 PredictionCritic 仍能正确接入 Nav2。
但 S1 Oracle 超时说明“接口接通”不等于“闭环收益成立”。

## 6. 当前 Gate 判断

当前仍然是：

```text
Gate 0: PASS
Gate 1: PASS（硬验收）
Gate 2: PASS（环境与证据链路）
Gate 3: PASS（Oracle 接口）
Gate 4: PASS（PredictionCritic 与零风险回归）
Gate 5: BLOCKED
Gate 6: 未开始
Transformer: 暂不允许开始
```

不能因为启动问题减少后、smoke 出现多次成功，就把 formal Gate 5 的失败样本
从分母中删除，也不能把当前 `cost_weight=50.0` 写成已验证最优值。

## 7. 下一步执行顺序

按照任务书，只改变一个变量。当前建议的诊断矩阵为：

```text
场景：S1_crossing、S2_oncoming
方法：Reactive 与 Oracle 配对
Oracle 权重：0、10、50
重复：每个场景、每个权重至少 3 个配对 run
Reactive：始终使用冻结的 nav2_mppi_reactive_10hz_params.yaml
```

先在当前提交 `a20b43c` 上完成该单变量扫频，记录：

- 目标结果、启动结果和 runner/trial exit code；
- Gazebo 动态 contacts；
- 最小机器人—障碍物距离；
- 首次风险命中与首次控制变化时间；
- stop/reverse 比例；
- 仿真时间、轨迹长度、末端误差；
- progress failure、no-valid-plan 和控制周期告警；
- Oracle publisher/critic active、stale、risk-hit 诊断。

扫频结束后，才根据预先定义的安全、完成性和提前响应规则选择候选权重，
再重新执行完整的正式 Gate 5。当前没有进入 Gate 6 或 Transformer 的授权。

