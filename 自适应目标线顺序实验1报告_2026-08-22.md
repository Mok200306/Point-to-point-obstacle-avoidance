# 自适应目标线顺序实验 1 报告

日期：2026-08-22
实验目录：`results/自适应目标线_顺序实验1_CADBMC_2026-08-22/`
冻结 profile：`adaptive_goal_line_045`
顺序：`C -> A -> D -> B -> M -> C`

## 1. 实验目的

在不改变算法参数的情况下，把起点从 M 改为 C，并改变五个点的访问顺序，验证规划器是否会针对每一段的实时起点、目标和在线代价地图重新规划，而不是记住原始 `M -> A -> B -> C -> D -> M` 路线。

本实验从 Gazebo 的 C 点 `(-5.0, 4.0)` 出生，依次发送 A、D、B、M、C 五个 `NavigateToPose` 目标。每次 run 都独立重启容器并使用 `reset_db=true`，RTAB-Map 从空数据库在线建图。

## 2. 固定条件

| 项目 | 值 |
| --- | --- |
| 世界 | `indoor_obstacle_course_large.world` |
| 起点 | C `(-5.0, 4.0)` |
| 目标顺序 | A `(5.0,-3.0)`、D `(0,0)`、B `(5,6)`、M `(-8.5,0)`、C `(-5,4)` |
| 建图模式 | `online=true`、`localization=false`、`reset_db=true` |
| 规划器 | `GoalLineSmacPlanner` / `SmacPlanner2D` |
| 目标线偏置 | 开启；每段使用当前 start/goal |
| 固定世界走廊偏置 | 关闭：`side_bias=false`、schedule=false |
| inflation | `0.45 m` |
| RPP | `0.28 m/s`，lookahead `0.62--1.20 m` |
| 安全层 | RGB-D `/camera/cloud` -> collision monitor -> `/cmd_vel_safe` |

## 3. 三次结果

标准差采用三次 run 的总体标准差，与项目历史报告保持一致。

| run | 总 wall [s] | 总轨迹 [m] | 分段成功 | 末端误差均值 [m] | 最大末端误差 [m] | contacts 消息 | 非地面接触 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 01 | 293.795 | 49.771 | 5/5 | 0.117 | 0.155 | 792,464 | none |
| 02 | 298.734 | 49.681 | 5/5 | 0.117 | 0.155 | 797,713 | none |
| 03 | 297.418 | 49.689 | 5/5 | 0.117 | 0.155 | 788,872 | none |
| 平均 +/- 标准差 | **296.649 +/- 2.088** | **49.714 +/- 0.041** | **15/15** | **0.117** | **0.155** | - | **0/3** |

`contacts 消息`是 Gazebo 物理接触话题中收到的原始消息数量，不等于碰撞次数。脚本过滤 `ground_plane` 后检查机器人与墙体、障碍栏、箱体和柱体的接触对，三次均为 `(none)`。

### 分段均值

| 分段 | wall [s] | 轨迹 [m] | 末端 XY 误差 [m] |
| --- | ---: | ---: | ---: |
| C -> A | 66.600 | 12.703 | 0.109 |
| A -> D | 39.486 | 5.832 | 0.105 |
| D -> B | 65.861 | 9.965 | 0.122 |
| B -> M | 85.980 | 15.621 | 0.125 |
| M -> C | 38.722 | 5.592 | 0.123 |

## 4. 轨迹证据

每个目录包含 `trajectory_comparison.png`：左侧为 Gazebo 真值俯视图，右侧为 RViz 风格 `/map` 和全局 costmap。彩色线表示真实记录的分段轨迹，黑色虚线只是该段实际起点到目标的参考线。

- [run_01 合成轨迹图](results/自适应目标线_顺序实验1_CADBMC_2026-08-22/run_01/trajectory_comparison.png)
- [run_02 合成轨迹图](results/自适应目标线_顺序实验1_CADBMC_2026-08-22/run_02/trajectory_comparison.png)
- [run_03 合成轨迹图](results/自适应目标线_顺序实验1_CADBMC_2026-08-22/run_03/trajectory_comparison.png)

每个 run 还保存 `多目标轨迹.csv`、`Gazebo真值多目标轨迹.csv`、五个分段 CSV、`metrics.yaml`、`多目标指标.yaml`、`实验参数.yaml`、`导航参数.yaml`、`碰撞监视参数.yaml` 和 `世界文件.sdf`。

## 5. 结论

本顺序三次独立运行成功率为 `3/3=100%`，分段成功率为 `15/15=100%`，过滤地面后的非地面物理接触为 `0/3`。不同于原始闭环，本实验的第一段是 `C->A`，并且图中后续每段的直线参考和实际弯绕方向随当前目标改变，支持“每段实时规划”的结论。

该结论的范围是当前大场景、当前 Waffle 仿真模型和当前 RGB-D 传感器条件；它证明 profile 在这一场景的多顺序鲁棒性，不等于对任意新障碍布局的保证。
