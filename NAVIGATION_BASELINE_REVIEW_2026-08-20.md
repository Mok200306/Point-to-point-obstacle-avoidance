# 导航基线与参数演化分析

更新时间：2026-08-20

本文只做现有结果和 Git 记录的复核，不修改导航代码，也不启动仿真。目的
是把“验收基线、实验候选、当前源码”分开，避免不同参数的结果被混写。

> 历史说明：本文形成于目标线插件之前，文中“当前源码为 0.55”只对应当时的
> clearance-first 阶段。当前源码和最新三次回归以
> [NAVIGATION_OPTIMIZATION_2026-08-20.md](NAVIGATION_OPTIMIZATION_2026-08-20.md)
> 与提交 `c18894e` 为准；原始 0.45 基线由 `be04483` 保留。

## 1. 先给结论

目前仓库中同时存在三类内容：

1. 旧版 `NavFn + DWB` 的完整历史基线：A -> B 成功，`182.87 s`，末端
   XY 误差 `0.108 m`。
2. 中间的 `Smac + RPP` 参数实验：`0.30` 失败，`0.45` 成功并且轨迹
   看起来更紧凑，`0.55 final` 成功但绕行更外。
3. 当前源码：`SmacPlanner2D + Regulated Pure Pursuit + 0.55 m`
   clearance-first 配置。它是后来的一套候选方案，不应自动取代你认为
   更好的 `0.45` 或此前报告的 `98 s` 结果。

因此，当前最重要的动作不是继续扩大 RPP 或 inflation，而是先冻结并确认
一套基线。没有相同配置的快照，就不能只根据目录名判断“0.45 比 0.55 好”
是否完全由 inflation radius 导致。

## 2. 当前可复核的结果

| 结果标签 | 规划器/控制器 | inflation | A -> B 状态 | 时间 | 末端 XY 误差 | 轨迹长度 | 结论 |
|---|---|---:|---:|---:|---:|---:|---|
| `A_to_B_clearance` | NavFn + DWB | `0.50 m` | `4` | `182.87 s` | `0.108 m` | 未在旧指标中记录 | 完整历史基线 |
| `smac_rpp_030_A_to_B` | Smac + RPP | `0.30 m` | `6` | `139.71 s` | `5.643 m` | `11.73 m` | 失败，不能验收 |
| `smac_rpp_045_A_to_B` | Smac + RPP | `0.45 m`* | `4` | `115.55 s` | `0.154 m` | `17.66 m` | 成功，轨迹较紧凑 |
| `smac_rpp_055_final_A_to_B` | Smac + RPP | `0.55 m` | `4` | `114.52 s` | `0.152 m` | `18.47 m` | 成功，但绕行更外 |

`0.45 m` 的数值来自结果目录命名。该目录被 `.gitignore` 忽略，仓库中没有
对应的参数快照，因此表中的规划器和 inflation 不能作为严格的可重复配置证明。
`0.55 final` 的 metrics、CSV、PNG 已经被 Git 保存。

你报告的“新配置 A -> B 成功、约 `98 s`、末端误差 `0.107 m`”比表中的
`0.45` 和 `0.55` 都好，应该作为最高优先级的待复现实验候选。但当前工作树
和 Git 历史中没有找到该数字对应的 `metrics.yaml`、轨迹目录或参数快照，暂时
不能把它写成已复核验收结果，也不能猜测它使用了哪一组 RPP 参数。

## 3. 为什么会感觉“越改越乱”

问题不只是某个参数，而是实验管理方式曾经同时改变了多个变量：

```text
NavFn -> SmacPlanner2D
DWB -> RPP
直接使用增长中的 /map -> 固定 /nav_map
inflation 0.30 -> 0.45 -> 0.55
行为树重规划周期也发生变化
```

这些变化分别影响路径拓扑、局部跟踪、地图更新延迟、障碍物代价和终点收敛。
当它们一起变化时，某次耗时变快或轨迹更好，不能归因到单个参数。

目前 Git 本地历史并没有丢掉两次主要源码阶段：

- `2bf62cf`：保存了旧版 `NavFn + DWB` 工作区；
- `2e52d3a`：保存了当前 `Smac + RPP` clearance-first 工作区。

但 `0.30`、`0.45` 和报告中的 `98 s` 结果目录没有被纳入 Git，且生成结果
默认被 `.gitignore` 忽略。这正是“代码能回退，但实验过程无法完整回放”的原因。

## 4. 当前规划到底是什么

当前源码的完整链路是：

```text
Gazebo RGB-D
  -> /camera/cloud、/camera/obstacles
  -> RTAB-Map /map
  -> map_padder.py -> 固定 /nav_map
  -> global costmap: StaticLayer + obstacle_layer + inflation_layer
  -> SmacPlanner2D: 全局代价感知路径
  -> Smac smoother: 平滑全局路径
  -> behavior tree: 路径失效/过期/目标变化时重规划
  -> RPP: 局部前视跟踪和速度调节
  -> velocity_smoother
  -> collision_monitor
  -> /cmd_vel_safe -> Gazebo
```

这仍然是“全局规划 + 局部控制”，并没有变成三个独立的规划器：

- `SmacPlanner2D` 决定从障碍物哪一侧绕行，以及全局路径经过哪些网格；
- `smoother` 只改善路径的连续性，不负责发现新的绕行拓扑；
- `RPP` 不重新寻找全局路线，它沿全局路径前视一个 carrot 点，按路径曲率、
  cost 和碰撞预测计算速度与角速度；
- `collision_monitor` 是最后的安全过滤器，不是路径规划器。

### RPP 为什么被引入

旧版 DWB 每个控制周期采样多组速度轨迹，再由多个 critic 评分。它在 RGB-D
地图刚更新、全局路径变化或窄角转弯时，可能出现候选轨迹反复被淘汰，表现为
停顿、左右小幅摆动或重新规划等待。

RPP 的优势是路径跟踪行为更确定：前视距离使机器人在路径拐点前开始转弯，
曲率调速让弯道减速，代价调速和前向碰撞预测负责近障碍时降速或停止。它能
改善“如何跟踪已经选好的路”，但不能保证全局路径本身就是最漂亮的路；全局
路径的质量仍主要由 costmap 和 Smac 参数决定。

所以 RPP 对本项目有帮助，但它不是越大越好，也不是每次感觉贴边就应该继续
扩大 lookahead。前视过大可能切弯、靠近障碍或错过窄口；前视过小则会晚转弯。
当前 `0.52--1.10 m` 应先冻结，等待基线比较后再动。

## 5. inflation radius 和 0.48 m 车宽的关系

当前硬 footprint 是：

```text
长 0.60 m，宽 0.48 m，padding 0.03 m
```

这不是用 `inflation_radius` 表示的。footprint 是真实碰撞几何，costmap 会用
它判断机器人矩形是否进入 lethal 障碍；`inflation_radius` 是障碍物周围的
代价传播范围，也就是一圈“软代价梯度”。

可以把它分成三层理解：

```text
实体障碍物 + footprint 重叠     -> 不可通行
footprint 外的 inflation 区域   -> 通常仍可能通行，但代价越来越高
inflation 区域之外              -> 代价较低，规划器更偏好
```

Waffle 加 padding 后，短边方向的内切半径约为 `0.27 m`。因此：

- `inflation_radius=0.30 m` 只比硬边界多约 `0.03 m` 的有效梯度，规划器很难
  区分“刚好擦边”和“更开阔”；这解释了贴边现象，但不能解释为“0.30 一定更快”；
- `0.55 m` 不是要求车体离障碍物固定保持 `0.55 m`，也不是把车宽变成
  `0.55 m`；它会让障碍物外更大范围的网格带有代价；
- 但是，当 `cost_travel_multiplier=6.0` 时，这个软代价会被 Smac 更强地累计，
  可能让合法但较窄的通道代价过高，导致机器人选择更远的路线，甚至在没有
  替代路线时表现得像“堵住”。

所以你对 `0.55` 的怀疑是合理的。它是一次 clearance-first 候选，不是普适的
推荐值。当前证据只能说明它在本次场景 A -> B、B -> A 成功且没有检测到物理
接触，不能说明它比 `0.45` 的视觉路径更好，也不能说明所有窄通道都应该使用它。

## 6. 为什么 0.45 的图看起来更好

从现有轨迹图和 CSV 看：

- `0.45` 的 A -> B 轨迹在绕过障碍时最大偏移约 `-1.55 m`，总轨迹约
  `17.66 m`；
- `0.55 final` 的 A -> B 轨迹最大偏移约 `-2.01 m`，总轨迹约 `18.47 m`；
- 两者耗时几乎相同，终点采样误差也接近。

因此 `0.55` 的主要变化不是“更快”，而是路径偏好更保守、更远离障碍。对于
当前这个通道宽度和目标位置，这种额外绕行没有明显的时间收益，视觉上自然可能
不如 `0.45`。

但由于 `0.45` 没有参数快照，不能严格断言差异只来自 `inflation_radius`；RPP
前视、cost scaling、重规划周期、初始地图状态或运行时机都可能同时不同。要把
`0.45` 作为论文基线，必须把当时完整 YAML 和启动参数找回来或重新生成。

## 7. 当前建议：先冻结，不继续扩展模块

在没有复现 `98 s` 之前，我建议按下面顺序管理，而不是继续调参：

1. 把报告中的 `98 s / 0.107 m` 标为“待复现候选”；不要覆盖它，也不要把
   `0.55` 自动称为最终方案。
2. 把已存在的 `0.45` 目录标为“成功候选”，但补齐它的完整参数快照。
3. 暂时保留当前源码作为 `Smac + RPP + 0.55` 对照，不再增加新的规划器或
   再扩大 RPP 前视。
4. 每次实验只改变一组参数，并同时跑 A -> B、B -> A；至少重复 3 次后才
   判断优劣。
5. 统一验收门槛：Nav2 action `status=4`、无非地面 Gazebo contacts、
   末端 XY 误差、总耗时、轨迹长度、最小障碍距离和停顿/重规划次数。

当前最合理的候选顺序是：

```text
候选 0：报告中的 98 s 配置（先复现）
候选 1：0.45 成功轨迹（补快照后复现）
候选 2：0.55 final（当前源码对照）
历史基线：NavFn + DWB，182.87 s
淘汰候选：0.30，本次 status=6
```

## 8. 以后每次大改动的保存规则

每个实验必须同时保存：

```text
experiments/<id>/nav2_rgbd_params.yaml
experiments/<id>/launch_args.txt
experiments/<id>/metrics.yaml
experiments/<id>/trajectory.csv
experiments/<id>/trajectory.png
experiments/<id>/README.md
```

`README.md` 至少写明：Git commit、世界文件、起点终点、控制器、规划器、
inflation、cost scaling、RPP 前视、速度、是否 GUI、是否重置数据库、成功标准。
代码或参数发生结构性变化时，先提交 Git，再开始实验；实验结束后再提交结果，
并使用清楚的标签，例如：

```text
baseline-navfn-dwb
candidate-smac-rpp-045
candidate-smac-rpp-055
```

这样以后可以回答“哪次改了什么、为什么变快、为什么路径变形、是否真的更安全”，
而不是只根据结果目录名称推测。

## 9. 当前状态的准确表述

当前项目已经有可运行的 RGB-D 在线建图、Smac 全局规划、RPP 局部跟踪和碰撞
过滤闭环；但“最优路径”还没有被证明。当前 `0.55` 是一个成功的安全偏好候选，
不是最终验收标准；`0.45` 是更值得复核的轨迹候选；报告中的 `98 s` 是最值得
优先恢复的候选结果；`0.30` 这次已被实际失败证据淘汰。
