# 五点闭环最终参数

冻结日期：2026-08-22
冻结 profile：`adaptive_goal_line_045`
适用范围：`indoor_obstacle_course_large.world` 的在线 RGB-D 五点闭环仿真
对应代码：`5cde1a6`（run_02、run_03）；run_01 的规划代码快照为 `de287e0`

## 冻结原则

本 profile 通过三次五点闭环回归：`3/3` run 成功、`15/15` 段成功、`0/3` 非地面
Gazebo contacts。后续跨场景实验必须先完整复制本参数，不应同时修改速度、膨胀、
规划器和安全层。只有在新场景验证失败后，才建立新的 profile 和新的三次回归目录。

run_01 到 run_03 之间没有改变规划参数；`5cde1a6` 主要修复了轨迹图的 ASCII 图例
显示，避免字体差异影响证据图。结果汇总见
[五点闭环导航实验记录_2026-08-22.md](五点闭环导航实验记录_2026-08-22.md)。

## 规划与控制

| 模块 | 参数 | 冻结值 |
| --- | --- | --- |
| 全局规划器 | plugin | `rtabmap_tb3_nav/GoalLineSmacPlanner`（SmacPlanner2D 子类） |
| 全局规划器 | `cost_travel_multiplier` | `6.0` |
| 目标线软偏好 | `line_bias_enabled` | `true` |
| 目标线软偏好 | `line_bias_max_cost` | `60.0` |
| 目标线软偏好 | `line_bias_distance_scale` | `2.0 m` |
| 目标线软偏好 | `line_bias_exponent` | `2.0` |
| 场景走廊先验 | `side_bias_enabled` | `false` |
| 场景走廊先验 | `side_bias_target_schedule_enabled` | `false` |
| RPP | `desired_linear_vel` | `0.28 m/s` |
| RPP | `lookahead_dist` | `0.80 m` |
| RPP | `min_lookahead_dist` | `0.62 m` |
| RPP | `max_lookahead_dist` | `1.20 m` |
| RPP | `lookahead_time` | `1.7 s` |
| RPP | `inflation_cost_scaling_factor` | `4.5` |
| RPP | `use_collision_detection` | `true` |
| 控制器 | `controller_frequency` | `15 Hz` |
| 速度平滑 | `max_velocity` | `[0.28, 0.0, 0.90]` |
| 速度平滑 | `max_accel` | `[0.9, 0.0, 2.0]` |
| 速度平滑 | `max_decel` | `[-1.1, 0.0, -2.0]` |
| 目标判定 | `xy_goal_tolerance` | `0.12 m` |
| 目标判定 | `yaw_goal_tolerance` | `0.15 rad` |

`line_bias` 是每次规划调用根据当前 start/goal 计算的软代价，不是保存路线；障碍物
的 lethal cell、footprint 和碰撞检测仍然优先。`side_bias` 全部关闭，因而不会把
当前大场景的某个固定 `world_x/world_y` 走廊带到其他地图。

## 代价地图与车体

| 模块 | 参数 | 冻结值 |
| --- | --- | --- |
| 机器人 footprint | 矩形尺寸 | `0.60 x 0.48 m` |
| footprint | `footprint_padding` | `0.03 m` |
| global/local inflation | `inflation_radius` | `0.45 m` |
| global/local inflation | YAML 基础 `cost_scaling_factor` | `3.0` |
| global/local inflation | profile 覆盖 `cost_scaling_factor` | `4.5` |
| global costmap | 尺寸 / 分辨率 | `24 x 17 m / 0.05 m` |
| local costmap | 尺寸 / 分辨率 | `6 x 5 m / 0.05 m` |
| RGB-D 障碍最大距离 | `obstacle_max_range` | `3.8 m` |
| RGB-D 障碍最大高度 | `max_obstacle_height` | `1.5 m` |

0.45 m 是障碍物外侧的软代价梯度，不是把车体尺寸改成 0.45 m；车体可行性仍由
footprint 和 lethal cost 约束。0.30 m 对当前 padding 后约 0.27 m 的内切半径只留下
很窄的软梯度，容易让路径贴边，因此本阶段继续冻结 0.45 m。

## 碰撞监视器

| 参数 | 冻结值 |
| --- | --- |
| 输入速度 | `/cmd_vel` |
| 输出速度 | `/cmd_vel_safe` |
| 点云输入 | `/camera/cloud` |
| `source_timeout` | `0.5 s` |
| `PolygonSlow` | 前方约 `1.05 m`，`slowdown_ratio=0.75` |
| `PolygonStop` | 前方约 `0.38 m`，`action_type=stop` |
| 运行状态 | `collision_monitor active [3]` |

Gazebo TurtleBot3 底盘订阅 `/cmd_vel_safe`。三次正式实验 contacts 监听均覆盖到最后
一个目标，过滤地面后无非地面接触。

## 启动与复现

```bash
cd /home/w417/RTAB-Map
sg docker -c './scripts/stop.sh'
sg docker -c './scripts/start.sh'
```

需要同时观察 Gazebo 和 RViz2 时，在启动后的终端运行：

```bash
sg docker -c './scripts/launch_demo.sh \
  gazebo_gui:=true rviz:=true rtabmap_viz:=false \
  online:=true localization:=false reset_db:=true \
  navigation_profile:=adaptive_goal_line_045'
```

正式五点回归使用唯一 label：

```bash
sg docker -c './scripts/multi_waypoint_regression.sh \
  --goal A:5.0:-3.0:0.0 \
  --goal B:5.0:6.0:0.0 \
  --goal C:-5.0:4.0:3.1415926 \
  --goal D:0.0:0.0:0.0 \
  --goal M:-8.5:0.0:3.1415926 \
  --start-name M --start-x -8.5 --start-y 0.0 \
  --label "自适应目标线_五点闭环_复现_2026-08-22" \
  --profile adaptive_goal_line_045 --settle-seconds 5'
```

结果写入 `results/自适应目标线_五点闭环_复现_2026-08-22/`，不会覆盖本阶段的
`run_01`、`run_02`、`run_03`。如果只修改目标坐标，不修改源码或 YAML，不需要重新
`colcon build`；如果修改 launch、Python、C++ 或 YAML，则重新构建工作区后再启动。

## 复现时的验收门槛

1. 所有请求分段均返回 `status=4`。
2. 每次 run 的 `gazebo_non_ground_contact` 必须为 `false`。
3. 保存 wall 时间、Gazebo 真值长度、map 长度、每段末端误差和完整 contacts 统计。
4. 生成左右合成 `trajectory_comparison.png`，不要使用同一个 label 覆盖旧结果。
5. 新环境若失败，先复制本文件建立新 profile，记录失败证据，再只修改一个因素。
