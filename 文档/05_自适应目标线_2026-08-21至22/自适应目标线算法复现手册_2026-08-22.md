# 自适应目标线算法复现手册

版本日期：2026-08-22
适用 profile：`adaptive_goal_line_045`
适用场景：`indoor_obstacle_course_large.world`
冻结代码基线：`de4ac49`
模式：在线 RGB-D SLAM + Nav2，无真实 LiDAR

## 1. 已冻结的算法定义

这套方法不是先保存一条路线，再让机器人按路线播放。每个目标都会创建一个新的
`NavigateToPose` action；规划器在收到目标时读取当前机器人位姿、当前目标和当前
代价地图，重新执行全局搜索。连续多目标时，下一段从上一段结束时的实际位姿开始。

### 在线建图与规划链路

```text
Gazebo RGB-D
  -> /camera/image_raw + /camera/depth/image_raw + /camera/points
  -> RTAB-Map online SLAM
  -> /map 和 map->odom

/camera/depth/image_raw
  -> point_cloud_xyz -> /camera/cloud
  -> collision_monitor -> /cmd_vel_safe -> Gazebo 底盘

/camera/cloud
  -> obstacles_detection
  -> /camera/obstacles
  -> Nav2 global/local costmap
  -> GoalLineSmacPlanner + RPP
  -> /cmd_vel -> collision_monitor
```

系统启动时 `online:=true`、`localization:=false`、`reset_db:=true`。RTAB-Map 从空数据库
开始边走边建立 `/map`；Nav2 同时使用固定尺寸的 `/nav_map` 接收不断增长的地图，并
通过 RGB-D obstacle layer 更新当前障碍。项目没有使用 `/scan`。

## 2. 规划器为什么不是固定坐标路线

全局规划器是 `rtabmap_tb3_nav/GoalLineSmacPlanner`，底层为 Nav2 `SmacPlanner2D`。
`line_bias` 只对“本次规划调用的当前起点到当前目标直线”施加二次软代价：

```text
line_bias_enabled = true
line_bias_max_cost = 60.0
line_bias_distance_scale = 2.0 m
line_bias_exponent = 2.0
```

它表达“在可行的前提下尽量朝目标方向走”，但不会穿过 lethal obstacle，也不会覆盖
机器人 footprint、膨胀层和碰撞检测。当前通用 profile 明确关闭了大场景专用的固定
走廊提示：

```text
side_bias_enabled = false
side_bias_target_schedule_enabled = false
side_bias_target_world_y_enabled = false
```

因此 M->A、A->B、B->C 等不同段的目标线不同，换目标顺序后每段的搜索问题也不同。
图中的黑色虚线是记录用的起点到终点参考线，不是控制器接收的预存轨迹。

## 3. 冻结参数

### 全局规划和局部控制

| 模块 | 参数 | 冻结值 |
| --- | --- | --- |
| planner | plugin | `GoalLineSmacPlanner` / `SmacPlanner2D` |
| planner | `cost_travel_multiplier` | `6.0` |
| planner | `max_planning_time` | `1.0 s` |
| goal-line | `line_bias_max_cost` | `60.0` |
| goal-line | `line_bias_distance_scale` | `2.0 m` |
| goal-line | `line_bias_exponent` | `2.0` |
| RPP | `desired_linear_vel` | `0.28 m/s` |
| RPP | lookahead | `0.62--1.20 m`，默认 `0.80 m` |
| RPP | `lookahead_time` | `1.7 s` |
| RPP | `inflation_cost_scaling_factor` | `4.5` |
| controller | `controller_frequency` | `15 Hz` |
| goal checker | XY tolerance | `0.12 m` |
| goal checker | yaw tolerance | `0.15 rad` |

### 车体、代价地图和安全层

| 模块 | 参数 | 冻结值 |
| --- | --- | --- |
| footprint | 矩形尺寸 | `0.60 x 0.48 m` |
| footprint | padding | `0.03 m` |
| global/local inflation | `inflation_radius` | `0.45 m` |
| global/local inflation | base `cost_scaling_factor` | `3.0` |
| global/local inflation | adaptive profile 覆盖值 | `4.5` |
| global costmap | 尺寸 / 分辨率 | `24 x 17 m / 0.05 m` |
| local costmap | 尺寸 / 分辨率 | `6 x 5 m / 0.05 m` |
| RGB-D obstacle | 最大深度范围 | `3.8 m` |
| collision monitor | slowdown 区 | 约 `1.05 m`，速度比例 `0.75` |
| collision monitor | stop 区 | 约 `0.38 m` |
| collision monitor | 输入 / 输出 | `/cmd_vel` -> `/cmd_vel_safe` |

`inflation_radius` 是障碍物外侧的软代价梯度，不是车体尺寸。硬约束仍由 footprint、
lethal cost、RPP 前向碰撞预测和 collision monitor 共同决定。

## 4. 从零复现

```bash
cd /home/w417/RTAB-Map
sg docker -c './scripts/stop.sh'
sg docker -c './scripts/start.sh'
sg docker -c './scripts/launch_demo.sh \
  world:=obstacle_course_large \
  x_pose:=-8.5 y_pose:=0.0 \
  gazebo_gui:=true rviz:=true rtabmap_viz:=false \
  online:=true localization:=false reset_db:=true \
  navigation_profile:=adaptive_goal_line_045'
```

保持 launch 终端运行，在第二个终端发送单个目标：

```bash
sg docker -c 'docker compose exec -T ros2 bash -lc \
  "source /opt/ros/humble/setup.bash && \
   source /workspaces/rtabmap_tb3_nav/install/setup.bash && \
   ros2 run rtabmap_tb3_nav send_goal.py --x 5.0 --y -3.0 --yaw 0.0"'
```

## 5. 复现任意多目标顺序

目标格式是 `NAME:X:Y:YAW`，可重复传入多个 `--goal`。例如原始五点闭环：

```bash
sg docker -c './scripts/multi_waypoint_regression.sh \
  --goal A:5.0:-3.0:0.0 \
  --goal B:5.0:6.0:0.0 \
  --goal C:-5.0:4.0:3.1415926 \
  --goal D:0.0:0.0:0.0 \
  --goal M:-8.5:0.0:3.1415926 \
  --start-name M --start-x -8.5 --start-y 0.0 \
  --label "自适应目标线_五点闭环_复现_唯一名称" \
  --profile adaptive_goal_line_045 --settle-seconds 5'
```

起点必须同时设置两处：launch 的 `x_pose/y_pose` 决定 Gazebo 出生位置，回归脚本的
`--start-x/--start-y` 只用于记录第一段直线和实验元数据。两者不一致时，实验记录的
黑线和统计会失真。

## 6. 结果和验收

每次必须使用新的 `--label`。结果目录会保存：

- `trajectory_comparison.png`：左侧 Gazebo 真值俯视图，右侧 RViz 风格 `/map` 和代价地图；
- `多目标轨迹.csv`、`Gazebo真值多目标轨迹.csv` 和每段 CSV；
- `metrics.yaml`、`多目标指标.yaml`：状态、时间、长度、末端误差；
- `导航参数.yaml`、`碰撞监视参数.yaml`、`实验参数.yaml`、`世界文件.sdf`；
- `gazebo_contact_messages`、`gazebo_non_ground_contact` 和接触对。

正式验收必须同时满足：所有段 `status=4`、无非地面 Gazebo contacts，并保存完整
轨迹图和参数快照。一次漂亮轨迹不能替代三次独立重启统计。

## 7. 代码位置

- launch/profile：`src/rtabmap_tb3_nav/launch/demo.launch.py`
- Nav2 参数：`src/rtabmap_tb3_nav/config/nav2_rgbd_params.yaml`
- 碰撞监视参数：`src/rtabmap_tb3_nav/config/collision_monitor_rgbd_params.yaml`
- 多目标回归入口：`scripts/multi_waypoint_regression.sh`
- 多目标记录和绘图：`src/rtabmap_tb3_nav/scripts/multi_waypoint_trial.py`
- 单段记录：`src/rtabmap_tb3_nav/scripts/navigation_trial.py`
