# 阶段 2：跨场景验证总览与当前状态

更新时间：2026-08-24
阶段状态：场景 01 已通过；场景 02 已完成正式验收；场景 03 尚未创建
当前推荐 profile：`adaptive_goal_line_045`（通用基线）
场景 02 最终 profile：`adaptive_goal_line_050_recovery_v13_line_tiebreaker`

## 1. 本文用途

本文是阶段 2 的长篇总览，统一说明本阶段为什么建立跨场景验证、场景 01 和场景 02
分别改变了什么、经历了哪些失败排查、当前保留哪些正式结果，以及下一位维护者应该
从哪里继续。结果原始证据位于 `results/05_跨场景验证/`；阶段文档按本目录的四个
子目录归档。

本次整理没有修改世界文件、导航源码或冻结参数，只整理结果和文档目录并修复入口。

## 2. 阶段 2 的共同实验原则

- 使用 Docker 内 Ubuntu 22.04 + ROS 2 Humble；主机为 Ubuntu 20.04 + RTX 4090。
- TurtleBot3 Waffle 使用模拟 RGB-D 相机，不订阅 `/scan`。
- RTAB-Map 以 `online:=true`、`localization:=false` 运行，机器人边走边建图。
- Nav2 使用 RTAB-Map 地图、RGB-D obstacle layer、GoalLineSmacPlanner 和 RPP。
- 每段目标由回归脚本在上一段完成后发送；规划器不接收预先录制的中间路径。
- 每次正式 run 都保存 `metrics.yaml`、双视角轨迹图、规划/真值 CSV、参数快照、
  世界快照和 Gazebo contacts。
- 验收条件是：全部目标段 `status=4`、非地面 contacts 为 0、数据完整；成功率不能
  只依据一张图或一次运行判断。

## 3. 场景 01：障碍布局改变后的五点闭环

世界文件：
`src/rtabmap_tb3_nav/worlds/indoor_obstacle_course_cross_scene_01.world`

场景 01 保留房间边界和机器人尺寸，改变障碍物的位置、方向和局部布局。三组点位
顺序分别为：

```text
M -> A -> B -> C -> D -> M
C -> A -> D -> B -> M -> C
B -> M -> A -> C -> D -> B
```

每组运行 3 次，共 9 次。结果为：

| 指标 | 结果 |
| --- | ---: |
| 完整 run | 9/9 |
| 目标段 | 45/45 |
| 非地面 Gazebo contacts | 0/9 |
| 结论 | 通过场景 01 仿真验收 |

三组平均 wall 时间分别约为 `292.229 s`、`288.647 s`、`342.412 s`。详细报告、参数
和轨迹索引见 [02_场景01](../02_场景01/README.md)。

## 4. 场景 02：交错障碍与四点闭环

世界文件：
`src/rtabmap_tb3_nav/worlds/indoor_obstacle_course_cross_scene_02.world`

用户确认了交错障碍布局，主要任务为：

```text
M(-8.5, 0.0) -> N(8.5, 0.0) -> X(-3.0, -4.0) -> Y(5.0, 5.0) -> M(-8.5, 0.0)
```

### 4.1 失败现象和原因

早期版本在 N→X 段出现过明显南侧长回环：机器人在约 `(2.5,-4.0)` 附近无法继续
前进时，没有立即在可行方向上重新建立朝向，而是先退回到约 `(6.0,-6.0)` 再掉头。
这不是脚本给了 `(6,-6)` 途经点，而是在线地图和局部代价在不同阶段产生了分支：

1. RGB-D 逐步观察使局部障碍轮廓短时间膨胀或变成未知；
2. RPP 的前向碰撞投影拒绝当前局部曲率；
3. `allow_reversing=false` 时，控制器不能把倒车当作常规轨迹自由度；
4. 旧的全局代价在重新规划时可能选中一条“先退回再绕行”的可行分支；
5. 恢复动作和周期重规划之间没有及时把路径拉回当前目标方向。

因此该现象是在线感知、全局分支选择、局部控制和恢复链路共同作用的结果，不能
简单归因于“背了固定路线”。

### 4.2 v13 最终验收

v13 profile 为 `adaptive_goal_line_050_recovery_v13_line_tiebreaker`。它保留实时规划
和安全层，采用约 6 秒周期重规划、轻量当前起点到目标直线软偏好，并关闭未知空间
目标进度偏置、额外未知惩罚和场景专用侧向先验。

| run | 总耗时 (s) | 总轨迹 (m) | 最大末端误差 (m) | 分段 | 非地面 contacts |
| --- | ---: | ---: | ---: | --- | --- |
| run_01 | 539.848 | 98.210 | 0.162 | 4/4 | none |
| run_02 | 540.191 | 98.042 | 0.162 | 4/4 | none |
| run_03 | 449.438 | 85.223 | 0.132 | 4/4 | none |
| 平均 | **509.826** | **93.825** | **0.162** | **12/12** | **0/3** |

N→X 三次轨迹长度为 `20.976--21.065 m`，没有再出现约 `y=-6` 的异常长回环。需要
明确：`509.826 s` 是三次平均值，不是每次固定耗时；M→N 的在线首次建图仍导致
较大时间和路径方差。

正式结果和复现手册见 [03_场景02_正式验收](../03_场景02_正式验收/README.md)。
早期失败分类见 [04_场景02_历史排查](../04_场景02_历史排查/README.md)。

## 5. 当前算法链路

```text
模拟 RGB-D 图像/深度
        -> RTAB-Map online SLAM -> /map、map->odom
        -> /nav_map + global/local costmap
        -> GoalLineSmacPlanner（Smac 栅格搜索 + 当前目标线软偏好）
        -> RPP 局部跟踪与速度调节
        -> collision_monitor 前方减速/停止保护
        -> TurtleBot3 Waffle
```

目标线只在每次规划调用时由当前起点和当前目标计算。黑色起终点直线只用于轨迹图
对照，不是固定控制轨迹。SDF 只交给 Gazebo 做物理仿真、交给绘图程序显示障碍物，
Nav2 不读取 SDF 作为先验地图。

## 6. 当前边界

当前证据支持的表述是：系统在两个设计好的静态 Gazebo 障碍场景中，能够用 RGB-D
在线建图、实时重规划、逐段点到点导航并通过物理接触检查。

当前证据不支持的表述是：任意陌生环境、动态行人、任意窄通道或真实 D435i 上都能
保证 100% 成功。真实设备还需要检查深度噪声、反光/黑色物体、相机安装 TF、底盘
里程计、速度接口和急停链路。

## 7. 下一步建议

1. 不要把场景 01、场景 02的正式指标与历史 pilot 混合统计。
2. 创建独立 `cross_scene_03.world`，只改变一个环境因素，先做单段 smoke test。
3. 使用当前冻结 profile 做 3 次完整实验，保存与场景 02相同的证据。
4. 若失败，先判断感知/TF、全局规划、局部控制、恢复或场景几何，再建立新 profile；
   不覆盖 v13 或 `adaptive_goal_line_045`。
5. 跨场景基线稳定后，再进入真实 D435i 的低速、空旷、静态障碍逐级测试。
