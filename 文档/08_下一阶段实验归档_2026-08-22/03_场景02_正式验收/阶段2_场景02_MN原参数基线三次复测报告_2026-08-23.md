# 阶段 2：场景 02 M→N 原参数基线三次复测

日期：2026-08-23
场景：`indoor_obstacle_course_cross_scene_02.world`
任务：`M(-8.5, 0.0) -> N(8.5, 0.0)`
profile：`adaptive_goal_line_045`
状态：**3/3 成功，0/3 非地面物理接触**

## 1. 复测目的

上一轮规范化世界复测出现 `2/3` 成功，其中一次在西侧第一道障碍前安全停止。按照本轮要求，先不修改原参数、不修改障碍物布局，重新独立启动 Gazebo、RTAB-Map 和 Nav2，验证失败是否只是偶发性。

本轮每次实验都满足：

- 在线 RGB-D RTAB-Map 建图与 Nav2 同时运行；
- 目标通过 `NavigateToPose` 发送，没有预置路线、障碍 waypoint 或录制地图回放；
- 每次独立重启仿真并重置 RTAB-Map 数据库；
- Gazebo contacts 过滤 `ground_plane` 后作为物理碰撞验收依据。

## 2. 结果汇总

| run | Nav2 状态 | wall 时间 (s) | 规划轨迹 (m) | Gazebo 轨迹 (m) | 末端误差 (m) | 非地面 contacts |
|---|---:|---:|---:|---:|---:|---|
| run_01 | 4 | 133.295 | 25.857 | 25.873 | 0.280 | none |
| run_02 | 4 | 155.541 | 25.893 | 25.884 | 0.105 | none |
| run_03 | 4 | 136.400 | 25.822 | 25.829 | 0.113 | none |
| 平均 ± 标准差 | 3/3 | **141.745 ± 12.048** | **25.857 ± 0.036** | **25.862 ± 0.029** | **0.166** | **0/3** |

成功率为 `100%`，失败率为 `0%`。三次 contacts 监听分别记录了 `305602`、`363548` 和 `305034` 条消息，过滤地面后均没有 `waffle` 与墙、栏杆、箱体或柱体的接触对。

## 3. 结论

原参数在本次独立复测中达到 3/3 成功，因此上一轮 `run_03` 的失败不能直接归因于算法必然失败，更可能是在线点云观测、局部代价地图增长和重规划时机共同造成的偶发分支。

这条基线只证明场景 02 的单段 M→N 已在当前 profile 下通过三次复测；它不等于四点闭环已经通过，也不等于对任意新场景具有 100% 保证。四点闭环使用的恢复增强 profile 另行记录，不能把两个 profile 的结果混为一谈。

## 4. 复现入口

结果目录：

```text
results/05_跨场景验证/场景02/01_正式验收/场景02_MN原参数基线_2026-08-23/run_01..run_03/
```

单段复现示例（先启动该世界，再在另一个终端运行回归脚本）：

```bash
cd /home/w417/RTAB-Map
sg docker -c './scripts/stop.sh'
sg docker -c './scripts/start.sh'
sg docker -c './scripts/launch_demo.sh gazebo_gui:=false rviz:=false rtabmap_viz:=false reset_db:=true online:=true localization:=false world_file:=/workspaces/rtabmap_tb3_nav/src/rtabmap_tb3_nav/worlds/indoor_obstacle_course_cross_scene_02.world navigation_profile:=adaptive_goal_line_045 x_pose:=-8.5 y_pose:=0.0'
sg docker -c './scripts/regression_leg.sh --x 8.5 --y 0.0 --yaw 0.0 --settle-seconds 5 --label 05_跨场景验证/场景02/复现_MN原参数 --profile adaptive_goal_line_045 --world-file src/rtabmap_tb3_nav/worlds/indoor_obstacle_course_cross_scene_02.world'
```

正式三次的完整命令和证据以各 `run_*` 目录中的 `实验参数.yaml`、`metrics.yaml`、世界文件快照及轨迹图为准。
